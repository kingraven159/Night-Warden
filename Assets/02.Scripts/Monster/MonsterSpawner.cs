using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum MonsterType { Nomal, Jumper, Spiter, Shleid, Charger }
public class MonsterSpawner : MonoBehaviour
{
   
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
